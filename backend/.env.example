# Copy to .env and adjust as needed.

APP_NAME=wewake
DEBUG=true

# SQLAlchemy-style URL. Default is a file-based SQLite db in backend/.
DATABASE_URL=sqlite:///./wewake.db

# Comma-separated list of allowed origins. "*" allows all (dev default).
CORS_ORIGINS=*
